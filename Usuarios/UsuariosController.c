#include "../libs/utils.h"
#include "../libs/inputs.h"

void controller_user(void) {
    // printf("Digite seu nome: ");
    // scanf("%[A-Za-z ]", nome);
    // limpa_buffer();
    // printf("\n");
    // printf("Digite seu CPF: ");
    // scanf("%[0-9.-]", cpf);
    // limpa_buffer();
    printf("\n");
    readPhone();
}